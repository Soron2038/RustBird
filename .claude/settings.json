{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/witt/Developer/RustBird && if ! git diff --quiet HEAD 2>/dev/null || [ -n \"$(git ls-files --others --exclude-standard 2>/dev/null)\" ]; then git add -A && git diff --cached --quiet 2>/dev/null || { FILES=$(git diff --cached --name-only 2>/dev/null | head -5 | paste -sd ', ' -) && git commit -m \"auto: ${FILES:-changes}\" && git push origin main; }; fi 2>/dev/null || true",
            "async": true,
            "statusMessage": "Auto-committing changes..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -qE '[.](svelte|ts)$' && cd /Users/witt/Developer/RustBird && npm run check 2>&1 | tail -25 || true",
            "timeout": 60,
            "statusMessage": "Type-checking Svelte/TS..."
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q '[.]rs$' && cd /Users/witt/Developer/RustBird && cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings 2>&1 | tail -30 || true",
            "timeout": 120,
            "statusMessage": "Running cargo clippy..."
          }
        ]
      }
    ]
  }
}
